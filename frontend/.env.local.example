# Backend origin used by the rewrite rule and the API client. Leave empty to
# call /api/* on the same origin (production deployment behind a reverse proxy).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Telegram bot username (without leading @). Used by the Telegram Login Widget
# script to know which bot to authenticate against.
NEXT_PUBLIC_BOT_USERNAME=
